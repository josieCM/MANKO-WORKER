Placeholder so the sessions directory is present in version control.
Each session gets sessions/<session_id>/chromium-profile/ (persistent Chromium profile).
NEVER commit the contents of these directories - they hold browser cookies and login state.